#include<vector>
#include<algorithm>
#include "task1.h"

int main() {
	/*
	char** test = { {'a', 'b', 'c'}, {'d', 'e', 'f'} };
	sort(test.begin(), )
	
	int test[4] = { 2, 3, 1, 5 };
	qsort(test, 4, sizeof(int), comp1);
	*/

	return 0;
}