#include "task1.h"

int main ()
{
/*    //  comp1
    int arr_comp1[] = { 6, 4, 9, 1, 3, 7, 5, 8, 2, 0 };
    int size_arr = sizeof(arr_comp1)/sizeof(int);
    qsort(arr_comp1, size_arr, sizeof(int), comp1);
    cout << "Comp1: ";
    for (int i = 0; i < size_arr; i++)
        cout << arr_comp1[i] << " ";

    //  comp2
    double arr_comp2[] = { 1213.045, 34.06, 17.83, 4.02, 0.76 };
    size_arr = sizeof(arr_comp2)/sizeof(double);
    qsort(arr_comp2, size_arr, sizeof(double), comp2);
    cout << "\nComp2: ";
    for (int i = 0; i < size_arr; i++)
        cout << arr_comp2[i] << " ";

    //  TODO: comp3
    char arr_comp3[][50] = { "c", "d", "a", "b", "f", };
    size_arr = 5;
    qsort(arr_comp3, size_arr, sizeof(char), comp3);
    cout << "\nComp3: ";
    for (int i = 0; i < size_arr; i++)
        cout << arr_comp3[i] << " ";

    cout << endl;*/

    return 0;
}